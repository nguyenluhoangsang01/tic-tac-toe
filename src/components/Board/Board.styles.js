import styled from "styled-components";

export const BoardWrapper = styled.div``;

export const Status = styled.div`
  margin-bottom: 15px;
  font-weight: 600;
  transition: all 0.2s;
  background-color: var(--ttt-textColor);
  padding: 6px 14px;
  border-radius: 6px;
  display: inline-block;
  max-width: 163px;

  span {
    color: var(--ttt-background);
  }
`;
export const BoardRow = styled.div``;
